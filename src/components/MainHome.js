import React, { useContext } from "react";

export const MainHome = () => {
  const quitSideBar = (e) => {
    e.preventDefault();
  };
  return (
    <div>
      <h1>HOLA</h1>
    </div>
  );
};
